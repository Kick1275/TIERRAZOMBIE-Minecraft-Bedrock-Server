
/*
import { world } from "@minecraft/server";
import { showGuardiaForm } from "../../../../../../Plugins/GameModeSystem.js";

world.afterEvents.playerInteractWithEntity.subscribe(ev => {
    if (ev.target.typeId === "tz:guardia_1") showGuardiaForm(ev.player);
});

world.afterEvents.entityHitEntity.subscribe(ev => {
    if (ev.damagingEntity?.typeId === "minecraft:player" && ev.hitEntity?.typeId === "tz:guardia_1")
        showGuardiaForm(ev.damagingEntity);
});

console.warn("[Guardia] Cargado");
*/
